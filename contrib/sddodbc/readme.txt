Name:                  ODBC
Environment variable:  HB_WITH_ODBC= (may only be needed on non-Windows systems)
Install (debian):      sudo apt-get install unixodbc-dev
Install (rpm):         unixodbc-devel
                       unixODBC-devel
Install (OS X):        brew install unixodbc
