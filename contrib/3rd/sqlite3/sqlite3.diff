--- sqlite3.orig/sqlite3.c	2026-08-26 14:20:42.428619837 +0200
+++ sqlite3/sqlite3.c	2026-08-26 23:10:34.588040172 +0200
@@ -14990,7 +14990,9 @@ struct fts5_api {
 /*
 ** Include standard header files as necessary
 */
+#if !defined(__BORLANDC__)
 #include <stdint.h>
+#endif
 #ifdef HAVE_INTTYPES_H
 #include <inttypes.h>
 #endif
@@ -15029,7 +15031,7 @@ struct fts5_api {
 ** Macros to hint to the compiler that a function should or should not be
 ** inlined.
 */
-#if defined(__GNUC__)
+#if defined(__GNUC__) && (__GNUC__>=4 || (__GNUC__==3 && __GNUC_MINOR__>=1))
 #  define SQLITE_NOINLINE  __attribute__((noinline))
 #  define SQLITE_INLINE    __attribute__((always_inline)) inline
 #elif defined(_MSC_VER) && _MSC_VER>=1310
@@ -15685,6 +15687,21 @@ SQLITE_PRIVATE void sqlite3HashClear(Has
                         int xxn=(N);while(xxn-->0)*(xxd++)=*(xxs++);}
 #endif
 
+#if defined( __BORLANDC__ )
+#  if __BORLANDC__ >= 0x530
+#     define _LL( num )           num##i64
+#     define UINT64_C( num )      num##ui64
+#  else
+#     define _LL( num )           num
+#     define UINT64_C( num )      num
+#  endif
+#  define INFINITY                (1.0e308 * 10.0)
+#elif defined( _MSC_VER )
+#  define _LL( num )   num
+#else
+#  define _LL( num )   num##LL
+#endif
+
 /*
 ** If compiling for a processor that lacks floating point support,
 ** substitute integer for floating-point
@@ -15694,7 +15711,7 @@ SQLITE_PRIVATE void sqlite3HashClear(Has
 # define float sqlite_int64
 # define fabs(X) ((X)<0?-(X):(X))
 # define sqlite3IsOverflow(X) 0
-# define INFINITY (9223372036854775807LL)
+# define INFINITY (_LL(9223372036854775807))
 # ifndef SQLITE_BIG_DBL
 #   define SQLITE_BIG_DBL (((sqlite3_int64)1)<<50)
 # endif
@@ -37123,12 +37140,12 @@ static int countLeadingZeros(u64 m){
   return __builtin_clzll(m);
 #else
   int n = 0;
-  if( m <= 0x00000000ffffffffULL) { n += 32; m <<= 32; }
-  if( m <= 0x0000ffffffffffffULL) { n += 16; m <<= 16; }
-  if( m <= 0x00ffffffffffffffULL) { n += 8;  m <<= 8;  }
-  if( m <= 0x0fffffffffffffffULL) { n += 4;  m <<= 4;  }
-  if( m <= 0x3fffffffffffffffULL) { n += 2;  m <<= 2;  }
-  if( m <= 0x7fffffffffffffffULL) { n += 1;            }
+  if( m <= UINT64_C(0x00000000ffffffff)) { n += 32; m <<= 32; }
+  if( m <= UINT64_C(0x0000ffffffffffff)) { n += 16; m <<= 16; }
+  if( m <= UINT64_C(0x00ffffffffffffff)) { n += 8;  m <<= 8;  }
+  if( m <= UINT64_C(0x0fffffffffffffff)) { n += 4;  m <<= 4;  }
+  if( m <= UINT64_C(0x3fffffffffffffff)) { n += 2;  m <<= 2;  }
+  if( m <= UINT64_C(0x7fffffffffffffff)) { n += 1;            }
   return n;
 #endif
 }
@@ -37798,13 +37815,13 @@ SQLITE_PRIVATE void sqlite3FpDecode(FpDe
   memcpy(&v,&r,8);
   e = (v>>52)&0x7ff;
   if( e==0x7ff ){
-    p->isSpecial = 1 + (v!=0x7ff0000000000000LL);
+    p->isSpecial = 1 + (v!=_LL(0x7ff0000000000000));
     p->n = 0;
     p->iDP = 0;
     p->z = p->zBuf;
     return;
   }
-  v &= 0x000fffffffffffffULL;
+  v &= UINT64_C(0x000fffffffffffff);
   if( e==0 ){
     int nn = countLeadingZeros(v);
     v <<= nn;
@@ -37928,7 +37945,7 @@ SQLITE_PRIVATE int sqlite3GetUInt32(cons
   int i;
   for(i=0; sqlite3Isdigit(z[i]); i++){
     v = v*10 + z[i] - '0';
-    if( v>4294967296LL ){ *pI = 0; return 0; }
+    if( v>_LL(4294967296) ){ *pI = 0; return 0; }
   }
   if( i==0 || z[i]!=0 ){ *pI = 0; return 0; }
   *pI = (u32)v;
@@ -44272,7 +44289,11 @@ static int fcntlSizeHint(unixFile *pFile
       ** This is a similar technique to that used by glibc on systems
       ** that do not have a real fallocate() call.
       */
+#if __minix
+      int nBlk = 4096;            /* MFS default; good enough for now */
+#else
       int nBlk = buf.st_blksize;  /* File-system block size */
+#endif
       int nWrite = 0;             /* Number of bytes written by seekAndWrite */
       i64 iWrite;                 /* Next offset to write to */
 
@@ -50955,7 +50976,7 @@ struct tm *__cdecl localtime(const time_
   SYSTEMTIME pTm;
   sqlite3_int64 t64;
   t64 = *t;
-  t64 = (t64 + 11644473600)*10000000;
+  t64 = (t64 + _LL(11644473600))*10000000;
   uTm.dwLowDateTime = (DWORD)(t64 & 0xFFFFFFFF);
   uTm.dwHighDateTime= (DWORD)(t64 >> 32);
   osFileTimeToLocalFileTime(&uTm,&lTm);
@@ -86483,7 +86504,7 @@ SQLITE_PRIVATE int sqlite3RealSameAsInt(
   double r2 = (double)i;
   return r1==0.0
       || (memcmp(&r1, &r2, sizeof(r1))==0
-          && i >= -2251799813685248LL && i < 2251799813685248LL);
+          && i >= _LL(-2251799813685248) && i < _LL(2251799813685248));
 }
 
 /* Convert a floating point value to its closest integer.  Do so in
@@ -92043,7 +92064,7 @@ SQLITE_PRIVATE void sqlite3VdbeSerialGet
       pMem->u.i = FOUR_BYTE_INT(buf);
 #ifdef __HP_cc
       /* Work around a sign-extension bug in the HP compiler for HP/UX */
-      if( buf[0]&0x80 ) pMem->u.i |= 0xffffffff80000000LL;
+      if( buf[0]&0x80 ) pMem->u.i |= _LL(0xffffffff80000000);
 #endif
       pMem->flags = MEM_Int;
       testcase( pMem->u.i<0 );
@@ -99961,11 +99982,11 @@ case OP_TypeCheck: {
             ** that will fit in 6 bytes, then change the type to MEM_IntReal
             ** so that we keep the high-resolution integer value but know that
             ** the type really wants to be REAL. */
-            testcase( pIn1->u.i==140737488355328LL );
-            testcase( pIn1->u.i==140737488355327LL );
-            testcase( pIn1->u.i==-140737488355328LL );
-            testcase( pIn1->u.i==-140737488355329LL );
-            if( pIn1->u.i<=140737488355327LL && pIn1->u.i>=-140737488355328LL){
+            testcase( pIn1->u.i==_LL(140737488355328) );
+            testcase( pIn1->u.i==_LL(140737488355327) );
+            testcase( pIn1->u.i==_LL(-140737488355328) );
+            testcase( pIn1->u.i==_LL(-140737488355329) );
+            if( pIn1->u.i<=_LL(140737488355327) && pIn1->u.i>=_LL(-140737488355328)){
               pIn1->flags |= MEM_IntReal;
               pIn1->flags &= ~MEM_Int;
             }else{
@@ -100024,11 +100045,11 @@ case OP_Affinity: {
       ** that will fit in 6 bytes, then change the type to MEM_IntReal
       ** so that we keep the high-resolution integer value but know that
       ** the type really wants to be REAL. */
-      testcase( pIn1->u.i==140737488355328LL );
-      testcase( pIn1->u.i==140737488355327LL );
-      testcase( pIn1->u.i==-140737488355328LL );
-      testcase( pIn1->u.i==-140737488355329LL );
-      if( pIn1->u.i<=140737488355327LL && pIn1->u.i>=-140737488355328LL ){
+      testcase( pIn1->u.i==_LL(140737488355328) );
+      testcase( pIn1->u.i==_LL(140737488355327) );
+      testcase( pIn1->u.i==_LL(-140737488355328) );
+      testcase( pIn1->u.i==_LL(-140737488355329) );
+      if( pIn1->u.i<=_LL(140737488355327) && pIn1->u.i>=_LL(-140737488355328) ){
         pIn1->flags |= MEM_IntReal;
         pIn1->flags &= ~MEM_Int;
       }else{
@@ -100210,8 +100231,8 @@ case OP_MakeRecord: {
       testcase( uu==127 );               testcase( uu==128 );
       testcase( uu==32767 );             testcase( uu==32768 );
       testcase( uu==8388607 );           testcase( uu==8388608 );
-      testcase( uu==2147483647 );        testcase( uu==2147483648LL );
-      testcase( uu==140737488355327LL ); testcase( uu==140737488355328LL );
+      testcase( uu==2147483647 );        testcase( uu==_LL(2147483648) );
+      testcase( uu==_LL(140737488355327) ); testcase( uu==_LL(140737488355328) );
       if( uu<=127 ){
         if( (i&1)==i && p->minWriteFileFormat>=4 ){
           pRec->uTemp = 8+(u32)uu;
@@ -100228,7 +100249,7 @@ case OP_MakeRecord: {
       }else if( uu<=2147483647 ){
         nData += 4;
         pRec->uTemp = 4;
-      }else if( uu<=140737488355327LL ){
+      }else if( uu<=_LL(140737488355327) ){
         nData += 6;
         pRec->uTemp = 5;
       }else{
@@ -107080,7 +107101,8 @@ static int vdbePmaReadBlob(
 
     /* Copy as much data as is available in the buffer into the start of
     ** p->aAlloc[].  */
-    memcpy(p->aAlloc, &p->aBuffer[iBuf], nAvail);
+    if( nAvail > 0 )
+      memcpy(p->aAlloc, &p->aBuffer[iBuf], nAvail);
     p->iReadOff += nAvail;
     nRem = nByte - nAvail;
 
@@ -126566,6 +126588,10 @@ SQLITE_PRIVATE void sqlite3AuthContextPo
 */
 /* #include "sqliteInt.h" */
 
+#if defined( __GNUC__ ) && __GNUC__ >= 14
+#  pragma GCC diagnostic ignored "-Wstringop-overread"
+#endif
+
 #ifndef SQLITE_OMIT_SHARED_CACHE
 /*
 ** The TableLock structure is only used by the sqlite3TableLock() and
@@ -135828,7 +135854,7 @@ static void kahanBabuskaNeumaierStep(
 ** Add a (possibly large) integer to the running sum.
 */
 static void kahanBabuskaNeumaierStepInt64(volatile SumCtx *pSum, i64 iVal){
-  if( iVal<=-4503599627370496LL || iVal>=+4503599627370496LL ){
+  if( iVal<=_LL(-4503599627370496) || iVal>=_LL(+4503599627370496) ){
     i64 iBig, iSm;
     iSm = iVal % 16384;
     iBig = iVal - iSm;
@@ -135846,7 +135872,7 @@ static void kahanBabuskaNeumaierInit(
   volatile SumCtx *p,
   i64 iVal
 ){
-  if( iVal<=-4503599627370496LL || iVal>=+4503599627370496LL ){
+  if( iVal<=_LL(-4503599627370496) || iVal>=_LL(+4503599627370496) ){
     i64 iSm = iVal % 16384;
     p->rSum = (double)(iVal - iSm);
     p->rErr = (double)iSm;
@@ -225215,9 +225241,9 @@ static void icuCaseFunc16(sqlite3_contex
     zOutput = zNew;
     status = U_ZERO_ERROR;
     if( bToUpper ){
-      nOut = 2LL*u_strToUpper(zOutput,nOut/2,zInput,nInput/2,zLocale,&status);
+      nOut = _LL(2)*u_strToUpper(zOutput,nOut/2,zInput,nInput/2,zLocale,&status);
     }else{
-      nOut = 2LL*u_strToLower(zOutput,nOut/2,zInput,nInput/2,zLocale,&status);
+      nOut = _LL(2)*u_strToLower(zOutput,nOut/2,zInput,nInput/2,zLocale,&status);
     }
 
     if( U_SUCCESS(status) ){
